const modalElement = document.getElementById("playlistModal");
const modal = bootstrap.Modal.getInstance(modalElement);
modal.hide();

// 🎵 Flashメッセージ（成功）を出すならここに追加してもOK
console.log("✅ プレイリスト作成完了 → モーダル閉じる");
