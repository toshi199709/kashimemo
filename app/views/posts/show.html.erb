<h1>🎬 投稿詳細</h1>

<div style="text-align: center;">
  <iframe width="560" height="315"
          src="<%= youtube_embed_url(@post.video_url) %>"
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen>
  </iframe>
</div>

<div style="margin-top: 20px;">
  <p><strong>🎵 歌詞:</strong></p>
  <p><%= simple_format(@post.lyrics) %></p>

  <p><strong>📝 メモ:</strong></p>
  <p><%= simple_format(@post.memo) %></p>
</div>

<div style="margin-top: 30px; text-align: center;">
  <% if user_signed_in? && current_user == @post.user %>
    <%= link_to "✏️ 編集する", edit_post_path(@post), class: "load-btn" %>
    <%= link_to "🗑️ 削除", post_path(@post), data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" }, class: "load-btn" %>
  <% end %>
</div>
