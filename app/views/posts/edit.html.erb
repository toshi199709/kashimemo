<h1>✏️ 投稿を編集</h1>

<%= render "form", post: @post %>

<%= link_to "← 戻る", session[:return_to] || mypage_path, class: "load-btn" %>

<div style="margin-top: 20px;">
  <%= link_to "🗑️ 削除する", post_path(@post), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "load-btn" %>
</div>
